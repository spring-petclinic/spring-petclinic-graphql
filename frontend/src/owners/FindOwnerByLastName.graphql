query FindOwnerByLastName($lastName: String) {
  owners(
    filter: { lastName: $lastName }
    orders: { field: lastName, order: ASC }
  ) {
    ...OwnerFields
    pets {
      id
      name
    }
  }
}
